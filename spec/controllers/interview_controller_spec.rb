require 'rails_helper'
require 'spec_helper'

RSpec.describe InterviewController, type: :controller do
  # it 'can get through with a 200 code' do
  #   post :create, :q => {:q => "intrex", :position => "dresses"}
  #   expect(response.code).to eq("200")
  # end
  #
  # it 'will still work even with only the company input' do
  #   post :create, :q => {:q => "intrex", :position => ""}
  #   expect(response.code).to eq("200")
  # end
  #
  # it 'will work only with the position input' do
  #   post :create, :q => {:q => "", :position => "dresses"}
  #   expect(response.code).to eq("200")
  # end
end
